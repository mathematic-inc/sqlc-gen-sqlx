version: "2"
plugins:
  - name: sqlx
    process:
      cmd: {{plugin_binary}}
sql:
  - engine: "postgresql"
    schema: "schema.sql"
    queries: "query.sql"
    codegen:
      - plugin: "sqlx"
        out: "src"
        options:
          output: "queries.rs"
